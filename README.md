# DebateHub (Public Debate Platform)

Monorepo with Spring Boot backend and Angular frontend.

## Folders
- `backend/` — Spring Boot REST API
- `frontend/` — Angular app
- `db/` — SQL schema & dumps

## Quick start
See per-folder READMEs.
